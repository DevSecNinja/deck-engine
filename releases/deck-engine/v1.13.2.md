# @deckio/deck-engine v1.13.2

## Patch Changes

- Isolate Vite optimized dependency caches per deck by default. This prevents shared `node_modules/.vite` chunk invalidation when multiple hosted deck previews run from shared workspace dependencies.
