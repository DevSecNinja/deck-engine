# @deckio/deck-engine v1.17.1

**Released:** 2026-05-17

## What changed

### Changed

- Teach the agent harness about `<EditableList>` across every surface (AGENTS.md, inline-editing instructions, deck-add-slide skill) so reorderable user-content lists are produced by default.
- Extract a single `descriptor-resolution.instructions.md` and replace duplicated Step 0 / theme / `designSystem` content in `slide-jsx`, `slide-css`, and `deck-add-slide`. One canonical source, no drift.
- Split repo-level `.github/copilot-instructions.md` (137 lines / 5.5 KB) into a lean 25-line overview plus a new `.github/RELEASING.md` carrying the full release procedure — large reduction in every-turn context for engine-repo Copilot sessions.
- Renumber `deck-add-slide/SKILL.md` steps after the merge (Steps 0–5).
- Fix stale `create-deck-project` references in release-notes guidance — package is `create-deckio`.

## Upgrade guide

No action required. Documentation-only patch — no API or runtime changes. Pair with `deck-launcher` v1.17.1 (system-prompt update) for the matching launcher-side harness improvements.

## Contributors

- @leandrolopezmsft
