# create-deckio v0.2.3

**Released:** 2026-03-18

## What changed

### Changed
- Run the `create-deckio` smoke test in the publish workflow before npm publish.

### Fixed
- Fix a CLI syntax error in the sample-content instructions so first-time users can run `npx create-deckio` successfully.
- Strengthen the release gate for first-run CLI regressions by validating the real non-interactive scaffold path in CI/CD.

## Upgrade guide

No action required.

## Contributors

- GitHub Copilot